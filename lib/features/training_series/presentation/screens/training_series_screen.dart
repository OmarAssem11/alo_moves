import 'package:flutter/material.dart';

class TrainingSeriesScreen extends StatelessWidget {
  const TrainingSeriesScreen();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
